specVersion: "v1beta"
serviceId: "datathon"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "datathon-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
